import { router } from "expo-router";
import { useState } from "react";
import { FlatList, Pressable, StyleSheet, Text, View } from "react-native";
import { BantuUsahaMark, BrandIcon, GrowthIllustration } from "@/components/brand-assets";
import { GradientPanel } from "@/components/gradient-panel";
import { ScreenContainer } from "@/components/screen-container";
import { guides, lessons } from "@/lib/business-content";
import { useBusinessStore } from "@/lib/business-store";
import { useThemeContext } from "@/lib/theme-provider";
type FeedItem = { kind: "guide"; id: string } | { kind: "lesson"; id: string };
export default function GuideScreen() { const { guideProgress, completedLessons } = useBusinessStore(); const { colorScheme } = useThemeContext(); const [section, setSection] = useState<"guide" | "lesson">("guide"); const dark = colorScheme === "dark"; const completedTotal = Object.values(guideProgress).reduce((total, values) => total + values.length, 0); const items: FeedItem[] = section === "guide" ? guides.map((item) => ({ kind: "guide", id: item.id })) : lessons.map((item) => ({ kind: "lesson", id: item.id })); return <ScreenContainer><FlatList data={items} keyExtractor={(item) => `${item.kind}-${item.id}`} contentContainerStyle={styles.list} ListHeaderComponent={<><View style={styles.topbar}><View style={styles.brand}><BantuUsahaMark size={27} /><View><Text style={[styles.overline, dark && styles.textDark]}>BELAJAR & PANDUAN</Text><Text style={[styles.title, dark && styles.textDark]}>Akademi Usaha</Text></View></View><Pressable onPress={() => router.push("/services")} style={({ pressed }) => [styles.bookmark, dark && styles.bookmarkDark, pressed && styles.pressed]} accessibilityRole="button"><BrandIcon name="book" size={20} color="#2D6EAE" /></Pressable></View><GradientPanel style={styles.hero} colors={["#1F548A", "#62A9CF"]}><View style={styles.heroContent}><Text style={styles.heroEyebrow}>UMKM KU ACADEMY</Text><Text style={styles.heroTitle}>Usaha jadi lebih mudah dipelajari.</Text><Text style={styles.heroText}>{completedTotal ? `${completedTotal} langkah sudah Anda praktikkan.` : "Mulai dari satu panduan praktis yang sesuai kondisi usaha Anda."}</Text><Pressable onPress={() => router.push("/readiness")} style={({ pressed }) => [styles.heroButton, pressed && styles.pressed]} accessibilityRole="button"><Text style={styles.heroButtonText}>Cari panduan saya</Text><BrandIcon name="arrow" size={16} color="#2D6EAE" /></Pressable></View><View style={styles.illustration}><GrowthIllustration /></View></GradientPanel><View style={[styles.tabs, dark && styles.tabsDark]}><Pressable onPress={() => setSection("guide")} style={[styles.tab, section === "guide" && styles.tabActive]} accessibilityRole="tab" accessibilityState={{ selected: section === "guide" }}><Text style={[styles.tabText, section === "guide" && styles.tabActiveText, dark && section !== "guide" && styles.textDark]}>Panduan Praktis</Text></Pressable><Pressable onPress={() => setSection("lesson")} style={[styles.tab, section === "lesson" && styles.tabActive]} accessibilityRole="tab" accessibilityState={{ selected: section === "lesson" }}><Text style={[styles.tabText, section === "lesson" && styles.tabActiveText, dark && section !== "lesson" && styles.textDark]}>Belajar Mandiri</Text></Pressable></View><Text style={[styles.sectionTitle, dark && styles.textDark]}>{section === "guide" ? "Mulai dari sini" : "Materi belajar mandiri"}</Text></>} renderItem={({ item, index }) => { if (item.kind === "lesson") { const lesson = lessons.find((entry) => entry.id === item.id)!; const done = completedLessons.includes(lesson.id); return <Pressable onPress={() => router.push(`/lesson/${lesson.id}`)} style={({ pressed }) => [styles.guideCard, dark && styles.cardDark, pressed && styles.pressed]} accessibilityRole="button"><View style={[styles.guideIcon, { backgroundColor: done ? "#E3F7F0" : "#EFEAFE" }]}><BrandIcon name={done ? "activity" : "academy"} size={23} color={done ? "#168B72" : "#5E4BB7"} /></View><View className="flex-1"><Text style={[styles.guideTitle, dark && styles.textDark]}>{lesson.title}</Text><Text style={[styles.guideSummary, dark && styles.mutedDark]}>{done ? "Sudah selesai" : lesson.duration}</Text></View><BrandIcon name="arrow" size={18} color="#2D6EAE" /></Pressable>; } const guide = guides.find((entry) => entry.id === item.id)!; const completed = guideProgress[guide.id]?.length ?? 0; const icons = ["guide", "academy", "permit", "activity"] as const; const tints = ["#E6F3FA", "#EFEAFE", "#FCE8EC", "#E3F7F0"]; const colors = ["#2D6EAE", "#5E4BB7", "#A84858", "#168B72"]; return <Pressable onPress={() => router.push(`/guide/${guide.id}`)} style={({ pressed }) => [styles.guideCard, dark && styles.cardDark, pressed && styles.pressed]} accessibilityRole="button"><View style={[styles.guideIcon, { backgroundColor: tints[index % 4] }]}><BrandIcon name={icons[index % 4]} size={23} color={colors[index % 4]} /></View><View className="flex-1"><Text style={[styles.guideTitle, dark && styles.textDark]}>{guide.title}</Text><Text style={[styles.guideSummary, dark && styles.mutedDark]} numberOfLines={2}>{guide.summary}</Text><Text style={styles.guideProgress}>{completed} dari {guide.steps.length} langkah selesai</Text></View><BrandIcon name="arrow" size={18} color="#2D6EAE" /></Pressable>; }} /></ScreenContainer>; }
const styles = StyleSheet.create({ list: { paddingHorizontal: 16, paddingBottom: 30 }, topbar: { paddingTop: 9, flexDirection: "row", alignItems: "center", justifyContent: "space-between" }, brand: { flexDirection: "row", alignItems: "center", gap: 7 }, overline: { color: "#8293A2", fontSize: 9, fontWeight: "900", letterSpacing: 0.8 }, title: { color: "#29435B", fontSize: 20, fontWeight: "900", letterSpacing: -0.3, marginTop: 1 }, bookmark: { width: 38, height: 38, borderRadius: 14, backgroundColor: "#F0F6FA", alignItems: "center", justifyContent: "center" }, bookmarkDark: { backgroundColor: "#172235", borderWidth: 1, borderColor: "#2A3B54" }, hero: { minHeight: 161, borderRadius: 21, marginTop: 16, padding: 17, backgroundColor: "#2D6EAE" }, heroContent: { width: "64%" }, heroEyebrow: { color: "#FFFFFF", fontSize: 9, fontWeight: "900", letterSpacing: 0.7 }, heroTitle: { color: "#FFFFFF", fontSize: 20, lineHeight: 25, fontWeight: "900", marginTop: 5 }, heroText: { color: "#FFFFFF", fontSize: 10, lineHeight: 15, marginTop: 6 }, heroButton: { alignSelf: "flex-start", height: 29, borderRadius: 15, paddingHorizontal: 10, marginTop: 12, backgroundColor: "#FFFFFF", flexDirection: "row", alignItems: "center", gap: 5 }, heroButtonText: { color: "#2D6EAE", fontSize: 10, fontWeight: "900" }, illustration: { position: "absolute", right: 2, bottom: 0 }, tabs: { height: 43, backgroundColor: "#FFFFFF", borderBottomWidth: 1, borderBottomColor: "#E2E9ED", flexDirection: "row", marginHorizontal: -16, marginTop: 10, paddingHorizontal: 16, gap: 28 }, tabsDark: { backgroundColor: "#101827", borderBottomColor: "#2A3B54" }, tab: { alignItems: "center", justifyContent: "center", paddingHorizontal: 1 }, tabActive: { borderBottomWidth: 2, borderBottomColor: "#2D6EAE" }, tabText: { color: "#7A8995", fontSize: 11, fontWeight: "800" }, tabActiveText: { color: "#2D6EAE", fontWeight: "900" }, sectionTitle: { color: "#2A3E52", fontSize: 15, fontWeight: "900", marginTop: 19, marginBottom: 9 }, guideCard: { minHeight: 83, backgroundColor: "#FFFFFF", borderRadius: 13, borderWidth: 1, borderColor: "#E2E9ED", padding: 11, marginBottom: 9, flexDirection: "row", alignItems: "center", gap: 10 }, cardDark: { backgroundColor: "#172235", borderColor: "#2A3B54" }, guideIcon: { width: 42, height: 42, borderRadius: 15, alignItems: "center", justifyContent: "center" }, guideTitle: { color: "#344B62", fontSize: 12, fontWeight: "900" }, guideSummary: { color: "#7B8B98", fontSize: 9, lineHeight: 13, marginTop: 3 }, guideProgress: { color: "#2D6EAE", fontSize: 9, fontWeight: "900", marginTop: 6 }, textDark: { color: "#FFFFFF" }, mutedDark: { color: "#FFFFFF" }, pressed: { opacity: 0.8, transform: [{ scale: 0.985 }] } });
